api_style: contrail
extends:
- base
id: feature
parents:
  global_system_config:
    description: list of features supported by the system.
    operations: CRUD
    presence: optional
plural: features
prefix: /
references:
  feature:
    description: list of features this feature is dependent on.
    operations: CRUD
    presence: optional
schema:
  properties: {}
  required: []
  type: object
