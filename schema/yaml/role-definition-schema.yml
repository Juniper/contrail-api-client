api_style: contrail
extends:
- base
id: role_definition
parents:
  global_system_config:
    description: list of role definitions supported by the system.
    operations: CRUD
    presence: optional
plural: role_definitions
prefix: /
references:
  feature:
    description: list of features for this role definition.
    operations: CRUD
    presence: optional
  overlay_role:
    description: overlay role for this role definition.
    operations: CRUD
    presence: optional
  physical_role:
    description: physical role for this role definition.
    operations: CRUD
    presence: optional
schema:
  properties: {}
  required: []
  type: object
